const devArea = {
    version: "01ab",
    production: true,
};

const client ={
    device: "web",
}

//pode-se exportar outras estruturas além de função
module.exports = {
    devArea,
    client,
};